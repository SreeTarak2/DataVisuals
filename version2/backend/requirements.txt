# ── Web framework & server ──
fastapi
uvicorn[standard]
python-multipart
python-dotenv

# ── Auth ──
bcrypt
passlib
python-jose[cryptography]
email-validator          # required at runtime by pydantic.EmailStr

# ── Database ──
motor                    # async MongoDB driver
pymongo                  # sync MongoDB driver (used in background pipeline tasks)
asyncpg                  # async PostgreSQL driver (used in PostgreSQL connector)

# ── Async / HTTP ──
aiofiles
httpx

# ── Data processing ──
numpy
pandas
polars
scipy
scikit-learn            # PCA, KMeans, IsolationForest for statistical analysis
duckdb                  # SQL query execution engine

# ── AI / Embeddings / Vector Search ──
sentence-transformers   # text embeddings for semantic search + belief store
langchain-huggingface   # HuggingFaceEmbeddings integration
langgraph               # QUIS agentic analysis pipeline
chromadb                # Belief Store (user knowledge graph)
faiss-cpu               # Vector index for dataset + query history search
tiktoken                # Token counting for LLM context management
rank-bm25               # Optional: BM25 hybrid search (lazy-imported)

# ── Rate limiting ──
slowapi

# ── Caching ──
redis                   # Optional: used in cache_service + WebSocket rate limiter
diskcache               # SQLite-backed persistent cache for DuckDB query results

# ── File handling ──
python-magic            # MIME type detection for uploaded files
charset-normalizer      # Encoding detection for CSV files (fallback to latin-1)

# ── PDF Generation ──
weasyprint
matplotlib

# ── Misc ──
requests                # CLI eval script (scripts/chatbot_eval_runner.py)
python-dateutil          # date parsing (services/pipeline/date_fixer.py)
rapidfuzz               # fuzzy category matching (services/pipeline/category_fixer.py)
